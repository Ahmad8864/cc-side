{
  "name": "cc-side",
  "version": "0.2.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "test": "bun test",
    "typecheck": "tsc -p .",
    "types:check": "bun scripts/check-mod-types.ts",
    "validate": "CLAUDE_CODE_ENABLE_FUNCTION_HOOKS=1 claude plugin validate .claude-plugin/plugin.json",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "bun run format:check && bun run types:check && bun run typecheck && bun test",
    "build": "bun scripts/build.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "prettier": "3.6.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.280"
  },
  "license": "MIT",
  "engines": {
    "bun": ">=1.2.23"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Ahmad8864/cc-side.git"
  }
}
